import styled from 'styled-components';
import { INDENTS } from '../../theme';

export const PostRequestStyled = styled.section`
  margin-top: ${INDENTS.section_margin};
`;

export const ErrorWrapper = styled.div`
  margin: 50px;
  padding: 14px;
  border-radius: 4px;
  background-color: #fed4d2;
`;
